# AUTOGENERATED by xops/mock/nginx.py — vhost for tff.local
server {
    listen 443 ssl;
    http2 on;
    server_name tff.local;

    ssl_certificate     /etc/nginx/certs/tff.local/chain.pem;
    ssl_certificate_key /etc/nginx/certs/tff.local/leaf.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;

    location / {
        proxy_pass http://mocksrv;
        proxy_set_header Host tff.local;
        proxy_set_header X-Negelir-Mock-Source $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

server {
    listen 80;
    server_name tff.local;
    return 301 https://$host$request_uri;
}
