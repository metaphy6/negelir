# AUTOGENERATED by xops/mock/nginx.py — do not edit by hand.
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    sendfile on;
    keepalive_timeout 65;
    server_tokens off;

    upstream mocksrv {
        server mocksrv:8090;
    }

    include /etc/nginx/conf.d/vhost-mackolik.local.conf;
    include /etc/nginx/conf.d/vhost-nesine.local.conf;
    include /etc/nginx/conf.d/vhost-tff.local.conf;
    include /etc/nginx/conf.d/vhost-openfootball.local.conf;
}
