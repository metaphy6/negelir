# Phase 16.39 — Definition of Done

> Extracted from `docs/planning/ROADMAP.md` §16.39
> as part of the phase-split modularization (mirrors the Phase 10
> pattern at `docs/design/phase10/sections/`). The `[ ]`/`[x]` state
> below is **binding**; the ROADMAP carries only the rollup.

### 16.39 Definition of Done

- [x] **Wrong-assumption ledger (§16.0)** — every retired assumption has at least one passing proof test; `xops/lint/phase16_ledger.py` green.
- [x] **Cross-phase coupling matrix (§16.16)** complete; lint refuses a PR touching a referenced sub-section without updating the table.
- [x] All six base planes plus the cross-phase planes (`feature_vectors`, `sec_quarantine`, `match_outcomes` alias, `calibration`, `competition`, `predict_invalidated`) emit NDJSON live feeds and hourly Parquet snapshots against the mock stack for ≥ 7 consecutive days with no integrity-sweep failures.
- [x] `FeedReader` is the only supported way for `swarm/*` to read ingested data; `test_swarm_isolation.py` (no `datasource.*` imports outside `FeedReader`), `test_no_db_imports.py` (no `psycopg`/`redis` under `swarm/`), and `test_no_bus_data_topics.py` (no swarm subscription to `scrape.raw`/`match.normalized`) all green in CI.
- [x] Bus-to-feeds cutover (§16.11) complete: `cfg.emitter_publish_v1=off` in dev and mock; documented rollback drill executed once successfully.
- [x] Backfill (§16.10) executed against the mock historical corpus; idempotency verified; promotion is byte-identical on a second run; live-emit-pause drill green.
- [x] All EMITTER §6.1 writer-guarantee tests green plus the §16.2 additions (single-writer lease, fsync, per-record CRC, disk-full backpressure, oversize truncation, per-source fairness floor, intra-day compaction, midnight-rotation crash recovery, clock-skew alert).
- [x] All EMITTER §7.3 storage tests green plus the §16.8 additions (etag verification, conditional PUT, retry budget, cost ledger, cold-start-uses-manifest). Object-lock test stays `xfail` until Phase 14.
- [x] All EMITTER §8.1 reader-contract tests green plus the §16.4 additions (cursor survives midnight rotation + intra-day part split, dedup LRU resurface signaled, lag metric, tombstone round-trip, apply-tombstones live view).
- [x] **Live wake-up control plane (§16.6)** green: pointer stream wakes readers; drop tolerance proven; reader falls back to poll when stream unavailable.
- [x] Versioned-schema coexistence verified (`score@v1` + `score@v2`); deprecation timer fires; **writer refuses EOL at startup** (ledger #9); field-removal shadow window honoured; alias view materializes `match_outcomes`.
- [x] All Phase 5 / 6 / 7 deferred Feed backends landed and parity-tested **with field-equal + prediction-equal-within-1e-9** (ledger #1): `FeatureSource` Feed backend, `CalibrationStore` Feed backend, `MatchOutcome` reader migration, `QuarantineSample` parquet plane, `feature_vectors.v1` plane available for Phase 6 KS-test.
- [x] **Per-region & per-league shard policy (§16.12)** live: region prefix present from day one (ledger #18); per-league partition round-trips; cross-region reads gated and audit-counted.
- [x] **Tombstones & cascade invalidation (§16.13)** live: freshness-retraction → tombstone within `cfg.emitter_tombstone_propagation_max_s`; `predict.invalidated.v1` dual-write atomic; `match_outcomes` excludes tombstones.
- [x] **Data classification (§16.14)** live: `data_class` envelope field required; mixed-class refusal; PII partition encryption-at-rest enforced where flagged.
- [x] **Reserved envelope fields (§16.15)** ship default-`null`; `verify_signature` passes-on-null; `field_provenance` populated on opt-in planes; lint enforces.
- [x] All §16.17 metrics exposed; alert rules pass `promtool check rules`; SLO budget test green in CI on the reference workload.
- [x] **DR drill (§16.18)**: restore-from-backup test green in CI; multi-emitter partition test green; `kill -9` test green.
- [x] Read ACL enforced with default-deny; secret-in-payload guard verified; audit log populated for human reads; PII record filtered by ACL.
- [x] Load test (§16.20) meets all five budgets; baseline recorded.
- [x] **Health, liveness, readiness & graceful shutdown (§16.21)** green: SIGTERM flips ready immediately; drain within `cfg.emitter_shutdown_grace_s`; thread-budget governor binding tested.
- [x] **Cost & egress budgets (§16.22)** live: per-driver cost ledger, egress ledger, KMS rate limit + TTL cache, cost panel in Phase 8 ops console.
- [x] **Chaos suite (§16.23)** green on the smallest CI lane: 9 chaos tests + concurrent reader×writer race; region-failover stays `xfail` until Phase 14.
- [x] **Tenant fairness (§16.24)** plumbing live: per-principal QPS, subscriber cap, decode-once snapshot buffer.
- [x] **Producer idempotency keys (§16.26)** required at writer entry; replay-5×-equals-1× test green; bloom dedup TTL honoured.
- [x] **Cross-plane snapshot consistency (§16.27)** live: `feeds.snapshot.ready.v1` published per `(plane, as_of)`; joined-snapshot reads refuse on partial readiness; cross-plane watermark exposed via `make feeds.watermark`.
- [x] **Right-to-erasure surface (§16.28)** wired end-to-end against the mock corpus: erasure request → tombstone → snapshot rebuild → key-shred (Phase 14 stub) → `erasure.completed.v1`; SLA test green; non-PII erasure refused; per-jurisdiction overlay honoured.
- [x] **PITR & manifest changelog (§16.29)** live: changelog append-only; periodic full snapshot every `cfg.feeds_manifest_snapshot_interval_min`; `make feeds.pitr.restore` rebuilds side-by-side; `time_travel(as_of=...)` reader passes determinism test.
- [x] **OpenTelemetry tracing (§16.30)** envelope-propagated end-to-end; writer/reader spans emitted; sampling honoured; error paths always sampled.
- [x] **Reader caching, bloom, memory bounds (§16.31)** live: per-process LRU + bloom sidecars halve re-decode CPU on the reference workload; memory governor refuses over-budget allocations rather than OOM-killing; tombstone-driven cache invalidation within `cfg.feed_reader_cache_invalidation_max_s`.
- [x] **Per-plane compression strategy (§16.32)** live: per-plane codec/level/dict honoured; ratio targets met (`market` ≥ 4× with dict, `editorial` ≥ 6×, `score` ≥ 3×); old generations remain readable post-rotation.
- [x] **Manifest sharding (§16.33)** live: per-`(plane, source)` shards + top-level index; index rebuild only on topology change; 32-writer concurrency test zero contention; one-time migration from monolithic manifest atomic.
- [x] **Cross-plane referential integrity (§16.34)** live: orphan `lineup`/`market`/`score` records quarantined; late-arriving `schedule` promotes orphans within `cfg.emitter_xref_promotion_max_s`; permanent orphans tombstoned after grace.
- [x] **Replay & time-travel (§16.35)** CLI byte-stable; trace-driven replay reconstructs read window for Phase 17 patcher diagnostic bundles; replay audit logged for non-public reads.
- [x] **Snapshot delta mode (§16.36)** live: deltas joined transparently on read; compaction byte-equivalent to full; ≥ 60 % disk savings vs full mode on the reference workload.
- [x] **Plane-level circuit breakers (§16.37)** live: per-`(plane, source)` quarantine-rate breaker; half-open recovery; per-plane quarantine quota; manual close audited.
- [x] **Schema-fingerprint coupling (§16.38)** live: writer pauses on `schema.fingerprint.changed.v1`; resumes on operator ACK or patcher promotion; pause queue bounded; audit chain intact.
- [x] All seven predictors + consensus + drift + proofreader replicas migrated to feeds with parity (field-equal + prediction-equal-within-1e-9) maintained on 8 weeks of mock history.
- [x] All 16 runbooks present and each maps to a named CI test.
- [x] **`xops/env/.env.example`** documents every new key with defaults that match `ai/common/config.py`: `NEGELIR_EMITTER_MAX_PAYLOAD_BYTES`, `NEGELIR_EMITTER_FSYNC_MODE`, `NEGELIR_EMITTER_FSYNC_BATCH_MS`, `NEGELIR_EMITTER_RECORD_CRC`, `NEGELIR_EMITTER_DISK_USAGE_WARN_PCT`, `NEGELIR_EMITTER_DISK_USAGE_BLOCK_PCT`, `NEGELIR_EMITTER_LEASE_RENEW_MS`, `NEGELIR_EMITTER_LEASE_TTL_MS`, `NEGELIR_EMITTER_PER_SOURCE_FLOOR_PCT`, `NEGELIR_EMITTER_SOURCE_BURST_FACTOR`, `NEGELIR_EMITTER_SOURCE_BURST_WINDOW_S`, `NEGELIR_EMITTER_CLOCK_SKEW_ALERT_MS`, `NEGELIR_EMITTER_INTRADAY_COMPACT_BYTES`, `NEGELIR_EMITTER_PARQUET_MAX_PART_BYTES`, `NEGELIR_EMITTER_SNAPSHOT_GRACE_MINUTES`, `NEGELIR_EMITTER_SNAPSHOT_INTERVAL_HOURS`, `NEGELIR_EMITTER_SNAPSHOT_WRITER_THREADS`, `NEGELIR_EMITTER_SNAPSHOT_MODE`, `NEGELIR_EMITTER_SNAPSHOT_COMPACTION_HOURS`, `NEGELIR_EMITTER_BACKFILL_BATCH_SIZE`, `NEGELIR_EMITTER_BACKFILL_MAX_RPS`, `NEGELIR_EMITTER_DUAL_WRITE_DAYS`, `NEGELIR_EMITTER_PUBLISH_V1`, `NEGELIR_EMITTER_TOMBSTONE_PROPAGATION_MAX_S`, `NEGELIR_EMITTER_POINTER_PUBLISH_MIN_INTERVAL_MS`, `NEGELIR_EMITTER_LIVENESS_BLOCK_THRESHOLD_MS`, `NEGELIR_EMITTER_FSYNC_QUEUE_MAX_DEPTH`, `NEGELIR_EMITTER_SHUTDOWN_GRACE_S`, `NEGELIR_EMITTER_THREAD_BUDGET`, `NEGELIR_EMITTER_DEFAULT_REGION`, `NEGELIR_EMITTER_MANAGEMENT_PORT`, `NEGELIR_EMITTER_MANIFEST_MAX_AGE_MS`, `NEGELIR_EMITTER_IDEMPOTENCY_BLOOM_SIZE`, `NEGELIR_EMITTER_IDEMPOTENCY_DEDUP_WINDOW_S`, `NEGELIR_EMITTER_XREF_WINDOW_H`, `NEGELIR_EMITTER_XREF_PROMOTION_MAX_S`, `NEGELIR_EMITTER_LEAGUE_BUCKET_COUNT`, `NEGELIR_EMITTER_FINGERPRINT_PAUSE_MAX_RECORDS`, `NEGELIR_EMITTER_CIRCUIT_QUARANTINE_WINDOW_S`, `NEGELIR_EMITTER_CIRCUIT_QUARANTINE_THRESHOLD`, `NEGELIR_EMITTER_CIRCUIT_HALF_OPEN_S`, `NEGELIR_EMITTER_CIRCUIT_MAX_OPEN_S`, `NEGELIR_EMITTER_QUARANTINE_MAX_PCT_PER_H`, `NEGELIR_EMITTER_COMPRESSION_CODEC_<PLANE>`, `NEGELIR_EMITTER_COMPRESSION_LEVEL_<PLANE>`, `NEGELIR_EMITTER_COMPRESSION_DICT_PATH_<PLANE>`, `NEGELIR_FEEDS_RETENTION_<PLANE>_NDJSON_DAYS`, `NEGELIR_FEEDS_RETENTION_<PLANE>_SNAPSHOT_DAYS`, `NEGELIR_FEEDS_SCHEMA_MAX_DEPRECATION_DAYS`, `NEGELIR_FEEDS_OBJECT_LOCK_MODE`, `NEGELIR_FEEDS_S3_KMS_KEY_ARN`, `NEGELIR_FEEDS_S3_LIST_FALLBACK_ENABLED`, `NEGELIR_FEEDS_STORE_RETRY_MAX_PER_OP`, `NEGELIR_FEEDS_STORE_RETRY_TOTAL_PER_MINUTE`, `NEGELIR_FEEDS_STORE_DAILY_USD_CAP`, `NEGELIR_FEEDS_KMS_DECRYPTS_PER_MINUTE_MAX`, `NEGELIR_FEEDS_KMS_DATA_KEY_TTL_S`, `NEGELIR_FEEDS_AT_REST_ENCRYPTION_ENABLED`, `NEGELIR_FEEDS_PARITY_CAPTURED_AT_JITTER_MS`, `NEGELIR_FEEDS_POINTER_STREAM_MAXLEN`, `NEGELIR_FEEDS_POINTER_MAX_SUBSCRIBERS_PER_PRINCIPAL`, `NEGELIR_FEEDS_SNAPSHOT_READY_STREAM_MAXLEN`, `NEGELIR_FEEDS_CROSS_REGION_MAX_STALENESS_S`, `NEGELIR_FEEDS_READER_DEFAULT_REGION`, `NEGELIR_FEEDS_SIGNING_ENABLED`, `NEGELIR_FEEDS_BLOOM_FPR_MAX`, `NEGELIR_FEEDS_INDEX_REFRESH_S`, `NEGELIR_FEEDS_MANIFEST_SNAPSHOT_INTERVAL_MIN`, `NEGELIR_FEEDS_MANIFEST_CHANGELOG_RETENTION_DAYS`, `NEGELIR_FEEDS_OTEL_ENABLED`, `NEGELIR_FEEDS_OTEL_ENDPOINT`, `NEGELIR_FEEDS_OTEL_SAMPLE_RATE`, `NEGELIR_FEEDS_ERASURE_SLA_H`, `NEGELIR_FEEDS_ERASURE_KEY_SHRED_HORIZON_DAYS`, `NEGELIR_FEEDS_JURISDICTION_ERASURE_OVERLAY`, `NEGELIR_FEED_READER_DEDUP_LRU_SIZE`, `NEGELIR_FEED_READER_TOMBSTONE_LRU_SIZE`, `NEGELIR_FEED_READER_MAX_LAG_MS`, `NEGELIR_FEED_READER_POLL_INTERVAL_MS`, `NEGELIR_FEED_READER_PER_PRINCIPAL_QPS_MAX`, `NEGELIR_FEED_READER_SNAPSHOT_BUFFER_MAX_MB`, `NEGELIR_FEED_READER_SNAPSHOT_CACHE_MAX_MB`, `NEGELIR_FEED_READER_PER_CALL_MAX_RESIDENT_MB`, `NEGELIR_FEED_READER_BATCH_ROWS`, `NEGELIR_FEED_READER_MEMORY_BUDGET_MB`, `NEGELIR_FEED_READER_CACHE_INVALIDATION_MAX_S`, `NEGELIR_SWARM_FEEDS_SHADOW_HOURS`, `NEGELIR_FIELD_REMOVAL_SHADOW_DAYS`.
- [x] `datasource_emitter` ≥ `1.0.0`; `common_feeds` ≥ `1.0.0`; `swarm` bumped to reflect the read-surface change; `ai` chart key still `active` (EOL deferred to Phase 18); `project` umbrella bumped on phase landing.

---
