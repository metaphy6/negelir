{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "negelir/swarm/sec.alert.v1/opsctl_operators_unreadable",
  "title": "sec.alert.v1{kind=opsctl_operators_unreadable}",
  "description": "Phase 8 §8.15.4 — opsctl_operators.json reload failed (file missing or corrupt). Consumer enters fail-safe: all signatures rejected. Bypass debounce. Severity: critical.",
  "type": "object",
  "required": ["alert_id", "kind", "severity", "source", "reason", "produced_at"],
  "properties": {
    "kind":     { "const": "opsctl_operators_unreadable" },
    "severity": { "const": "critical" },
    "source":   { "type": "string" },
    "reason":   { "type": "string" },
    "alert_id": { "type": "string" },
    "produced_at": { "type": "string" }
  }
}
