# Phase 18.4 — Deprecation stub for docker-compose.mock.yml
#
# This file is deprecated and will be removed in Phase 22.
# The 'mock' profile is now integrated into the main docker-compose.yml.
#
# Migration path:
#   OLD: docker compose -f docker-compose.yml -f docker-compose.mock.yml up
#   NEW: docker compose --profile core,mock up
#
# See docs/design/COMPONENT_LAYOUT.md §4 for profile documentation.

version: "3.8"

# Placeholder to render YAML-valid but non-functional
services:
  _deprecation_notice:
    image: busybox:latest
    entrypoint: /bin/sh
    command:
      - -c
      - |
        echo "ERROR: docker-compose.mock.yml is deprecated as of Phase 18.4"
        echo ""
        echo "This overlay file will be removed in Phase 22."
        echo "Use the 'mock' profile instead:"
        echo ""
        echo "  docker compose --profile core,mock up"
        echo ""
        echo "For more information, see:"
        echo "  docs/design/COMPONENT_LAYOUT.md §4"
        echo "  docs/design/DATA_SOURCE.md §5"
        echo ""
        exit 1
