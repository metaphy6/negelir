{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Match Outcomes Payload (v1) — Match-outcomes-plane entity",
  "description": "Phase 16 — Terminal match outcomes (realized at status=finished). This is an alias view over the score plane's terminal rows per EMITTER.md §3.3. Used by the drift agent.",
  "type": "object",
  "required": [
    "match_stable_id",
    "home_score",
    "away_score",
    "status"
  ],
  "additionalProperties": false,
  "properties": {
    "match_stable_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier for the match."
    },
    "home_score": {
      "type": "integer",
      "minimum": 0,
      "description": "Final goals scored by home team."
    },
    "away_score": {
      "type": "integer",
      "minimum": 0,
      "description": "Final goals scored by away team."
    },
    "status": {
      "type": "string",
      "enum": ["finished", "abandoned"],
      "description": "Terminal match status."
    },
    "finished_at": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "ISO-8601 UTC when the match ended."
    }
  }
}
