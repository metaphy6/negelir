{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "negelir/swarm/maint.event.v1/allowlist_approve",
  "title": "maint.event.v1{kind=allowlist_approve}",
  "description": "Phase 8 §8.1 / §8.7 — operator promotes a pattern_allowlist row from state='p' (pending) to 'a' (active). `target` is '<source>:<rule_id>'. Consumer: maint.sec.v1 (lands in §8.7).",
  "type": "object",
  "required": ["kind", "target", "request_id", "client_id", "produced_at"],
  "properties": {
    "kind":        { "const": "allowlist_approve" },
    "target":      { "type": "string", "minLength": 3, "pattern": "^[a-zA-Z0-9_.-]+:[a-zA-Z0-9_.-]+$", "description": "'<source>:<rule_id>' per §8.7 allowlist row addressing." },
    "request_id":  { "type": "string", "minLength": 1 },
    "client_id":   { "type": "string", "minLength": 1 },
    "produced_at": { "type": "string" },
    "reason":      { "type": "string", "description": "Free-text justification for the promotion (recorded on the audit row)." }
  },
  "additionalProperties": false
}
