"""Centralized SemVer chart for the Negelir project.

The single source of truth lives in ``xops/versioning/chart.yaml`` and is
managed exclusively through the ``version.py`` CLI in this package. Per
AGENTS.md §versioning, every change to a tracked component must be paired
with a ``bump`` invocation in the same commit.
"""
