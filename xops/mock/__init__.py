"""xops/mock — Phase 2 dev-stack helpers.

* ``capture.py`` — one-shot real-internet scraper (manual only).
* ``verify.py``  — offline integrity check (runs in CI).
* ``manifest.py`` — schema + load/save helpers shared by both.
"""
