{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Feature Vectors Payload (v1) — Feature-vectors-plane entity",
  "description": "Phase 16 — ML feature vectors per fixture (Phase 6 KS-test deferred). This plane carries the feature payload for model input.",
  "type": "object",
  "required": [
    "match_stable_id",
    "feature_id",
    "values"
  ],
  "additionalProperties": false,
  "properties": {
    "match_stable_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier for the match."
    },
    "feature_id": {
      "type": "string",
      "minLength": 1,
      "description": "Feature identifier or source."
    },
    "values": {
      "type": "object",
      "additionalProperties": {
        "type": ["number", "null"]
      },
      "description": "Computed feature values (feature name -> numeric value or null)."
    },
    "computed_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC when the features were computed."
    }
  }
}
