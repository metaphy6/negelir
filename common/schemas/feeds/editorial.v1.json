{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Editorial Payload (v1) — Editorial-plane entity",
  "description": "Phase 16 — News articles, columns, narratives. Per EMITTER.md §3.3, body_text is already ad-stripped (DATA_PIPELINE §5.3). Append-mostly with occasional edits flagged by updated timestamps.",
  "type": "object",
  "required": [
    "url",
    "title",
    "body_text",
    "published_at"
  ],
  "additionalProperties": false,
  "properties": {
    "url": {
      "type": "string",
      "format": "uri",
      "minLength": 1,
      "description": "Original source URL."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Article headline."
    },
    "body_text": {
      "type": "string",
      "minLength": 1,
      "description": "Main content, already ad-stripped (no embedded links or multimedia references)."
    },
    "authors": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Author names if available."
    },
    "published_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC original publication time."
    },
    "edited_at": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "ISO-8601 UTC when the article was last edited (null if never edited)."
    },
    "language": {
      "type": "string",
      "enum": ["tr", "en"],
      "description": "Article language code."
    }
  }
}
