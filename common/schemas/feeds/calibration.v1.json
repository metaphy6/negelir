{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Calibration Payload (v1) — Calibration-plane entity",
  "description": "Phase 16 — Calibration store records per Phase 5 §5.4. The CalibrationStore Protocol implementation ships here. Records describe per-competition calibration parameters.",
  "type": "object",
  "required": [
    "competition_id",
    "calibration_profile_id"
  ],
  "additionalProperties": false,
  "properties": {
    "competition_id": {
      "type": "string",
      "minLength": 1,
      "description": "Competition identifier."
    },
    "calibration_profile_id": {
      "type": "string",
      "minLength": 1,
      "description": "Identifier for the calibration profile to use."
    },
    "parameters": {
      "type": "object",
      "additionalProperties": true,
      "description": "Per-competition calibration parameters (model coefficients, thresholds, etc.)."
    },
    "effective_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC when this calibration became effective."
    }
  }
}
