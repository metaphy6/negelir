{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Schedule Payload (v1) — Schedule-plane entity",
  "description": "Phase 16 — Fixture records (schedules, postponements, kick-off times). Per EMITTER.md §3.3, status transitions are captured as new Records, not edits. Invariants ensure kickoff_utc consistency.",
  "type": "object",
  "required": [
    "match_stable_id",
    "kickoff_utc",
    "home",
    "away",
    "competition",
    "status"
  ],
  "additionalProperties": false,
  "properties": {
    "match_stable_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier for the match (used for joins with score, lineup, etc.)."
    },
    "kickoff_utc": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC scheduled or rescheduled kick-off time."
    },
    "home": {
      "type": "object",
      "required": ["team_id"],
      "additionalProperties": false,
      "properties": {
        "team_id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable ID for the home team."
        },
        "team_name": {
          "type": ["string", "null"],
          "description": "Display name (may differ from reference plane)."
        }
      }
    },
    "away": {
      "type": "object",
      "required": ["team_id"],
      "additionalProperties": false,
      "properties": {
        "team_id": {
          "type": "string",
          "minLength": 1,
          "description": "Stable ID for the away team."
        },
        "team_name": {
          "type": ["string", "null"],
          "description": "Display name (may differ from reference plane)."
        }
      }
    },
    "competition": {
      "type": "string",
      "minLength": 1,
      "description": "Competition ID (references competition plane)."
    },
    "status": {
      "type": "string",
      "enum": ["scheduled", "postponed", "cancelled", "finished"],
      "description": "Match lifecycle state."
    },
    "referee": {
      "type": ["string", "null"],
      "description": "Referee name or ID if available."
    },
    "venue": {
      "type": ["string", "null"],
      "description": "Venue name or ID if available."
    }
  }
}
