{
  "//": "Cursor project-scope MCP config. Codegraph entry pinned to npx (no global install).",
  "mcpServers": {
    "codegraph": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@colbymchenry/codegraph@0.8.0", "serve", "--mcp", "--path", "${workspaceFolder}"]
    }
  }
}
